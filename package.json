{  
  "name": "revengedns",  
  "version": "2.0.0",  
  "description": "Production-grade cross-platform defensive DNS security system",  
  "main": "dist/index.js",  
  "scripts": {  
    "build": "tsc",  
    "start": "node dist/index.js",  
    "dev": "tsc && node dist/index.js",  
    "clean": "rm -rf dist"  
  },  
  "keywords": [  
    "dns",  
    "security",  
    "adblock",  
    "malware",  
    "phishing"  
  ],  
  "author": "",  
  "license": "MIT",  
  "dependencies": {},  
  "devDependencies": {  
    "@types/node": "^20.10.0",  
    "typescript": "^5.3.0"  
  },  
  "engines": {  
    "node": ">=18.0.0"  
  }  
}